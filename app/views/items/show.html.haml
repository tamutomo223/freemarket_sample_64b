= render "header"
= render "sell"
.bar
.show-wrapper
  .item
    .item__show 
      = render "show_table"

    .bar2
    = render "show_comment"

    .item__link
      = link_to "#" do
        .item__link__prev
          < 中日ドラゴンズファンブック
      = link_to "#" do
        .item__link__next
          ポケモン図鑑 >
    .item__sns
      .item__sns__facebook
        = fa_icon "facebook-official"
      .item__sns__twitter 
        = fa_icon "twitter"
      .item__sns__pinterest  
        = fa_icon "pinterest"
    .bar3    
    .item__other
      .item__other__user
        .item__other__user__title
          = link_to "#" do
            %h2 おれさんのその他の出品
        .item__other__user__contents    
          = render "show_items"
          = render "show_items"
          = render "show_items"
          = render "show_items"
          = render "show_items"
          = render "show_items"
      .bar3          
      .item-other__user
        .item__other__user__title
          = link_to "#" do
            %h2 スニーカーその他の出品
        .item__other__user__contents  
          = render "show_items"
          = render "show_items"
          = render "show_items"
          = render "show_items"
          = render "show_items"
          = render "show_items"
  .bar        
  .bread
= render "footer"